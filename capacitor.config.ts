import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.literandltx.timerapp',
  appName: 'timer-app',
  webDir: 'dist/timer-angular/browser'
};

export default config;
